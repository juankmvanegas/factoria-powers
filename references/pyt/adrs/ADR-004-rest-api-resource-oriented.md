# ADR-004: REST Resource-Oriented API

## Status

Accepted

## Decision

The public interface of the backend is HTTP REST with resource-oriented routes and semantic HTTP verbs.

## Consequences

- Endpoints are modeled around resources
- Routes use kebab-case
- The API contract remains understandable and consistent
